@model RMDS.Models.UserDonation

@{
    ViewBag.Title = "EditDonation";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>EditDonation</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
<div class="form-horizontal">
    <h4>UserDonation</h4>

    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <div class="form-group">
       
        <div class="col-md-10">
            @Html.HiddenFor(model => model.DonationID, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.DonationID, "", new { @class = "text-danger" })
        </div>
    </div>
    <hr />
    <div class="form-group">
       <div class="col-md-10">
            @Html.HiddenFor(model => model.RequestDate)

        </div>
    </div>
    <hr />
    <div class="form-group">
        @Html.LabelFor(model => model.DonationStatus, htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @*<select class="form-control" name="@Model.DonationStatus" value="@Model.DonationStatus">
                    <option value="">Select a Status</option>
                    <option value="Pending">Pending</option>
                    <option value="Processed">Processed</option>
                    <option value="Cancel">Cancel</option>
        </select>*@
            @Html.DropDownListFor(model => model.DonationStatus, new SelectList(new string[] { "Pending", "Processed", "Cancel" }, Model.DonationStatus) , new { @class = " form-control dropdown-toggle", style=" height:35px"})

            @*@Html.DropDownListFor(m => m.StudentGender,
        new SelectList(Enum.GetValues(typeof(Gender))),
        "Select Gender")*@
            @Html.ValidationMessageFor(model => model.DonationStatus, "", new { @class = "text-danger" })
        </div>
    </div>
    <hr />
    <div class="form-group">
      
        <div class="col-md-10">
            @Html.HiddenFor(model => model.UserId)

        </div>
    </div>
    <hr />
    <div class="form-group">

        <div class="col-md-10">
            @Html.HiddenFor(model => model.DonationTypeId, new { htmlAttributes = new { @class = "form-control" } })

        </div>
    </div>




    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Edit Donation" class="btn btn-default" />
        </div>
    </div>
</div>
}

<div>
    @Html.ActionLink("Back to List", "GetAllPendingDonation")
</div>
